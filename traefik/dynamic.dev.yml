# Dev routes — host processes (Vite + uvicorn) and TLS for *.localhost.

http:
  routers:
    fast-nuxt-backend-host:
      rule: Host(`api.localhost`)
      entryPoints:
        - http
        - https
      service: fast-nuxt-backend-host
    fast-nuxt-frontend-host:
      rule: Host(`dashboard.localhost`)
      entryPoints:
        - http
        - https
      service: fast-nuxt-frontend-host

  services:
    fast-nuxt-backend-host:
      loadBalancer:
        servers:
          - url: http://host.docker.internal:8000
    fast-nuxt-frontend-host:
      loadBalancer:
        servers:
          - url: http://host.docker.internal:3100

tls:
  stores:
    default:
      defaultGeneratedCert:
        resolver: default
        domain:
          main: localhost
          sans:
            - "*.localhost"
